@model IEnumerable<Mvc_472_PortfolioC.Models.Comment>

@{
    ViewBag.Title = "Index";
}

@section testingSections
{
    @Html.ActionLink("Edit", "Edit", new { id = 1 }) |
    @Html.ActionLink("Details", "Details", new { id = 1 }) |
    @Html.ActionLink("Delete", "Delete", new { id = 1 })
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.Raw(item.Comments)
                @*@Html.DisplayFor(modelItem => item.Comments)*@
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
